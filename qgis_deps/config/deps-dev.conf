#!/usr/bin/env bash

export VERSION_major_python=3.10
export VERSION_minor_python=2
export VERSION_QT=5.15.2
export QGIS_DEPS_RELEASE_VERSION=dev
export QGIS_DEPS_RELEASE_VERSION_PATCH=dev
export MACOSX_DEPLOYMENT_TARGET=10.13.0

export WITH_ECW="true"
export WITH_MRSID="true"

### ! DO NOT EDIT UNLESS DEV ###
export QGIS_DEPS_SDK_VERSION=${QGIS_DEPS_RELEASE_VERSION}.${QGIS_DEPS_RELEASE_VERSION_PATCH}

export ROOT_QT_PATH=/opt/Qt/
export QT_BASE=${ROOT_QT_PATH}/${VERSION_QT}
export QT_PACKAGE_PATH=${ROOT_OUT_PATH}/qt-${VERSION_QT}.tar.gz

export ROOT_OPT_PATH=/opt/QGIS
export ROOT_OUT_PATH=${ROOT_OPT_PATH}/qgis-deps-${QGIS_DEPS_RELEASE_VERSION}
export QGIS_DEPS_PACKAGE_PATH=${ROOT_OUT_PATH}/qgis-deps-${QGIS_DEPS_SDK_VERSION}.tar.gz
export STAGE_PATH=${ROOT_OUT_PATH}/stage
export BUILD_PATH=${ROOT_OUT_PATH}/build
export PACKAGES_PATH=${PACKAGES_PATH:-${ROOT_OUT_PATH}/.packages}
